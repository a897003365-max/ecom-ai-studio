# ecom AI Studio 统一详细线稿

## 唯一入口

打开 `workbench-home-layout-options.html`。工作台与全站详细页已经合并到这一个 HTML，不再依赖第二个线稿文件。

统一线稿包含 26 个视图：

- 工作台首页及其 9 个诊断、行动与数据边界视图
- 运营数据看板及其 7 个 PowerBI 独有诊断视图
- 商品资产、文案与分镜、图片处理工坊、竞品情报、任务队列、商品管理、系统设置、用户与权限

左侧统一使用全站 10 个产品模块的工作栏。“工作台首页”的下钻页使用可折叠子导航：首页和其他产品模块默认收起，进入工作台下钻页时自动展开，也可以手动收起。

视觉默认采用深色经营指挥台：黄绿表示主操作与积极结果，琥珀、青蓝、紫色、粉红和红色分别承载收入、销量、利润、退款与风险信号；浅色模式保留为评审辅助，可从左侧“外观”切换。信息进入动效只执行一次，并支持 `prefers-reduced-motion`。

工作台首页采用：

`净回款结果 → 目标与预测差距 → 经营结构 → 过程诊断 → 流程优先级 → 行动 → 结果回看`

所有首页判断都有下钻入口。数据不足时，页面进入“数据条件”，只说明需要补充的数据及用途，不生成缺少依据的长期建议。

`workbench-home-layout-analysis.md` 记录经营口径、归因边界、各页面职责、交互要求和后续数据对象。

## 统一展示规则

- 经营页面顶部必须显示统计周期、对比口径、经营范围和数据截至时间。
- 当前累计目标差额与月末预计缺口必须分开表达。
- 先展示结果，再展示结构、过程原因、流程优先级和行动。
- 缺失数据与业务值 `0` 分开表达。
- 局部覆盖数据不得直接解释全渠道结果。
- 建议必须绑定依据、覆盖范围和可执行动作。
- 任务必须绑定明确业务实体、负责人、截止时间、完成标准与结果回看。
- 正式模式与演示模式共用后端数据契约，不在同一页面混用静态和实时数据。
- 响应式验收覆盖 `1440 / 1280 / 1024 / 768 / 390 / 375px`；页面根级不得横向溢出，导航和明细表允许在组件内部滚动。

## 附：商品画册视觉线稿（2026-08）

商品管理 → 商品画册 2026-08 视觉重设计（深色画廊数据标本卡）的独立 SVG 线稿，深色底可直接打开：

- `product-gallery-wireframe-page.svg`：整页结构（intro 去壳 / toolbar / 4 列网格 / 分页，含断点与 accent 纪律标注）
- `product-gallery-wireframe-card.svg`：单卡解剖（1:1 主图、排名 tag、趋势 pill、实收大数字、销量毛利率双格 + 规格标注 + 无图占位符）
- `product-gallery-wireframe-drawer.svg`：详情抽屉四分区（身份区 / 核心 KPI / 次要指标区 / SKU 表格）
