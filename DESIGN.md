# ecom AI Studio 设计规范

## 1. 产品与页面定位

ecom AI Studio 是面向电商运营、内容生产和经营分析人员的企业级工作台。页面首先帮助用户看清经营状态、异常、依据与下一步动作，其次才是视觉表现。现有深色运营看板、黄绿色强调色和高信息密度是产品识别的一部分，后续原型与实现应延续这套语言。

## 2. 设计风格

关键词：`克制`、`专业`、`数据密集`、`深色工作台`、`清晰层级`、`可追溯状态`。

- 使用低饱和深色表面、细边框和有限强调色建立层级。
- 动效只用于状态变化、反馈和数据进入，不做持续装饰。
- 优先展示业务判断、数据口径和可执行动作，不展示内部设计说明。
- 禁止夸张渐变、大面积毛玻璃、过度圆角、强烈投影、霓虹描边和无业务意义的漂浮卡片。

## 3. 页面与栅格

- 页面内容最大宽度：`1600px`，使用 `--layout-page-max` / `max-w-page`。
- 主内容采用移动优先的流式布局；桌面常用 12 栏概念栅格，平板收敛为 6 栏，移动端为 1 栏。
- 页面水平安全边距：移动端 16px、平板 20px、桌面 24px。
- 模块间距默认 20px，卡片内部间距默认 16px；数据密集型表格可使用 12px。
- Flex/Grid 子项必须设置合理的 `min-width: 0`；宽表放入横向滚动容器，不挤压数据列。
- 不为匹配截图大量使用绝对定位，也不使用固定高度裁切动态内容。

## 4. 颜色系统

源码以 `src/styles/tokens.css` 为唯一 Token 定义入口。Tailwind 使用 `tailwind.config.cjs` 中映射后的语义名称。

| 用途 | CSS Token | Tailwind 示例 |
| --- | --- | --- |
| 页面背景 | `--color-bg-canvas` | `bg-canvas` |
| 次级背景 | `--color-bg-subtle` | `bg-surface-subtle` |
| 卡片表面 | `--color-surface` | `bg-surface` |
| 强调表面 | `--color-surface-strong` | `bg-surface-strong` |
| 主文字 | `--color-text-primary` | `text-primary` |
| 次文字 | `--color-text-secondary` | `text-secondary` |
| 弱文字 | `--color-text-muted` | `text-muted` |
| 默认边框 | `--color-border-default` | `border-default` |
| 强边框 | `--color-border-strong` | `border-strong` |
| 品牌强调 | `--color-accent` | `bg-accent` / `text-accent` |

状态颜色仅表达含义：成功/正常使用绿色，进行中/信息使用蓝色，提醒使用橙色，失败/高风险使用红色，补充分类可使用紫色。不能只依赖颜色传达状态，必须同时提供文字、图标或形状。

## 5. 字体系统

- 正文：系统中文无衬线字体栈，使用 `--font-sans`。
- 标题、KPI：Space Grotesk + 中文系统字体，使用 `--font-display`。
- 数字：启用 `tabular-nums`，保证表格和 KPI 对齐。

字号层级：

| 层级 | Token | 建议用途 |
| --- | --- | --- |
| 11px | `--font-size-caption` | 辅助信息、时间戳 |
| 12px | `--font-size-small` | 标签、紧凑按钮 |
| 13.5px | `--font-size-body` | 正文、表格 |
| 14px | `--font-size-section` | Section 标题 |
| 16px | `--font-size-card-title` | 卡片大标题 |
| 20px | `--font-size-title-sm` | 移动端页标题 |
| 26px | `--font-size-title` | 桌面页标题 |
| 32px | `--font-size-kpi` | 关键 KPI |

字重只使用 400、500、600、700；正文默认 400，控件和小标题 600，页面标题与关键数字最多 700。

## 6. 圆角、阴影与间距

- 控件圆角：5px；卡片圆角：8px；浮层圆角：12px。
- 阴影只用于区分层级：卡片使用 `--shadow-card`，浮层使用 `--shadow-overlay`。
- 基础间距为 4px，优先使用 4/8/12/16/20/24/32/40/48px。
- 任意值必须有截图、图表几何或业务密度依据；避免重复出现 `w-[1173px]`、`mt-[37px]` 一类孤立数值。

## 7. 组件规范

### Card / Section

- 卡片必须有清晰的信息主题；同级卡片使用一致的内边距和标题位置。
- 可交互卡片必须有 hover、focus-visible 和按下反馈；静态卡片不添加误导性的悬浮效果。
- Section 用于业务层级，Card 用于视觉分组，不用卡片无限嵌套。

### Button

- 主操作每个局部区域最多一个；危险操作必须有明确文案与确认。
- 按钮最小点击高度 32px，移动端重要操作建议 40px。
- 图标按钮必须有可访问名称，不用只有颜色的禁用态。

### Form

- Input、Select、日期与筛选控件共享高度、边框、焦点环和错误状态。
- 标签不能只放在 placeholder 中；错误信息紧邻控件。
- 筛选栏允许换行，移动端优先保留时间、渠道和核心动作。

### Tabs

- 标签数量多时允许横向滚动，不压缩到不可读。
- 激活态同时使用文字、颜色和下划线/背景差异。

### Table

- 表格放入 `TableShell` 或同等横向滚动容器。
- 数值右对齐并使用等宽数字；列宽依据内容，不固定整表高度。
- 移动端不强行把宽表变成窄表；保留关键列并允许横向滚动。

### Modal / 状态页面

- Modal 仅承载需要中断主流程的操作；关闭、焦点和滚动行为必须明确。
- EmptyState 说明为什么为空和下一步；LoadingState 保持布局稳定；ErrorState 提供恢复动作。

### KPI / Chart / Filter / Page Header

- KPI Card 包含指标名、数值、时间/口径和必要的变化方向。
- Chart Card 必须有标题、口径、单位、图例和数据不足状态；移动端图表高度建议 220–320px。
- Filter Bar 可换行，控件顺序按业务优先级排列。
- Page Header 包含页面标题、业务说明和有限的页面级动作。

## 8. 响应式规则

实现断点使用移动优先策略：

- 基础 `< 640px`：单列、16px 边距、操作换行、宽表滚动。
- `sm 640px`：较宽移动设备与小平板。
- `md 768px`：平板，可启用双列但不得牺牲可读性。
- `lg 1024px`：小桌面，显示完整工作区布局。
- `xl 1280px`：标准桌面。
- `2xl 1440px`：宽桌面；控制行长，不无意义拉伸。
- `3xl 1600px`：达到页面最大宽度后居中。

每次页面视觉调整至少检查 1440、1280、1024、768、390、375px。重点检查横向溢出、卡片换行、图表高度、导航拥挤、文本截断、筛选换行、按钮越界和固定高度裁切。移动端优先显示结论、异常、核心 KPI 和主要动作，辅助解释可后置。

## 9. Open Design 原型落地规则

1. 开始前同时阅读本文件和 `design/DESIGN.md`；本文件定义长期系统，后者记录当前原型差异。
2. 原型只决定视觉结构和表现，不覆盖业务逻辑、接口、权限、路由、数据结构和数据口径。
3. 优先复用现有 `src/components` 组件和既有样式；新增组件时区分基础 UI 与业务组件。
4. 原型中的颜色、间距和尺寸先映射到 Token；只有图表坐标、数据驱动宽度等场景保留动态行内样式。
5. 先实现语义结构和响应式，再做像素微调；不得以单一截图宽度作为完成标准。
6. 保留清晰组件名、className 和 `data-ui`，使 Codex、Piny 与 DevTools 能追溯到源码。
7. 原型与现有功能冲突时，以现有功能、数据口径、安全规则和 `AGENTS.md` 为准，并在 `design/notes/` 记录差异。

