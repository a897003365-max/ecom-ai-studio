# 钉钉在线表格 读写完整指南

> 基于 `麻大师` 项目实战经验整理 | 2026-07-11

---

## 目录

1. [两种接入方式对比](#1-两种接入方式对比)
2. [DingTalk Sheet API v1.0（HTTP 直连）](#2-dingtalk-sheet-api-v10http-直连)
3. [认证流程](#3-认证流程)
4. [API 端点详解](#4-api-端点详解)
5. [关键约束与踩坑记录](#5-关键约束与踩坑记录)
6. [代码实现解析](#6-代码实现解析)
7. [配置速查](#7-配置速查)
8. [定时任务与通知](#8-定时任务与通知)
9. [常见问题排查](#9-常见问题排查)

---

## 1. 两种接入方式对比

| 维度 | MCP Server (`dingtalk-mcp`) | HTTP 直连 (Sheet API) |
|------|---------------------------|----------------------|
| 协议 | MCP (Model Context Protocol) | RESTful HTTP |
| 稳定性 | 有 schema bug，需要手动修复 | 稳定，直接调用 |
| 读写能力 | 多维表格（Notable）CRUD | 电子表格（Sheet）范围读写 |
| 适用场景 | 结构化数据记录级操作 | 单元格级批量读写 |
| 本项目使用 | ❌ 已弃用（schema 问题反复出现） | ✅ 主力方案 |

**结论**：本项目选择 HTTP 直连 Sheet API，绕过 MCP 中间层，直接操作钉钉在线电子表格。

---

## 2. DingTalk Sheet API v1.0（HTTP 直连）

### 2.1 基本概念

钉钉在线电子表格通过 **Sheet API** 操作，核心模型：

```
Workbook (工作簿)
  └── Sheet (子表/工作表)
        └── Range (单元格范围，如 "A1:Z100")
```

### 2.2 API Base URL

```
https://api.dingtalk.com/v1.0
```

### 2.3 三个核心端点

| 操作 | Method | URL 模板 |
|------|--------|---------|
| 列出子表 | `GET` | `/doc/workbooks/{workbookId}/sheets?operatorId={operatorId}` |
| 读取范围 | `GET` | `/doc/workbooks/{workbookId}/sheets/{sheetId}/ranges/{range}?operatorId={operatorId}` |
| 写入范围 | `PUT` | `/doc/workbooks/{workbookId}/sheets/{sheetId}/ranges/{range}?operatorId={operatorId}` |

### 2.4 请求/响应格式

#### 列出子表 — Response

```json
{
  "value": [
    { "name": "抖音", "id": "6jsXBA" },
    { "name": "京东pop", "id": "3ybhSX" },
    { "name": "京东自营", "id": "4XopNE" },
    { "name": "天猫旗舰店", "id": "1qKXCR" }
  ]
}
```

#### 读取范围 — Response

```json
{
  "values": [
    ["日期", "销售额", "订单数"],
    ["2026/06/15", "12345", "67"]
  ],
  "displayValues": [...],
  "backgroundColors": [
    ["#FFFFFF", "#FFFFFF", "#FFFFFF"],
    ["#FFFFFF", "#FFF2CC", "#FFFFFF"]
  ]
}
```

- `values` — 实际存储的值（日期为字符串格式 `YYYY/MM/DD`）
- `displayValues` — 显示值（可能含格式化）
- `backgroundColors` — 每个单元格的背景色 hex

#### 写入范围 — Request Body

```json
{
  "values": [
    ["2026/06/15", "12345", "67"]
  ],
  "backgroundColors": [
    ["#000000", "#FFF2CC", "#000000"]
  ]
}
```

- `values` 所有值**必须是字符串**
- `backgroundColors` 可选，格式 `#RRGGBB`

---

## 3. 认证流程

### 3.1 获取 Access Token

```
POST https://api.dingtalk.com/v1.0/oauth2/accessToken
Content-Type: application/json

{
  "appKey": "${DINGTALK_APP_KEY}",
  "appSecret": "${DINGTALK_APP_SECRET}"
}
```

**Response:**
```json
{
  "accessToken": "xxxxxxxx",
  "expireIn": 7200
}
```

### 3.2 使用 Token

所有后续 API 请求携带 HTTP Header：

```
x-acs-dingtalk-access-token: {accessToken}
```

### 3.3 operatorId

每个 API 请求都需要 `operatorId` 作为 query parameter，标识操作者身份。

| 字段 | 值 |
|------|-----|
| 用户 | 麦子卓 |
| userId | `${DINGTALK_OPERATOR_USER_ID}` |
| unionId | `${DINGTALK_OPERATOR_ID}` |

---

## 4. API 端点详解

### 4.1 列出所有子表

```python
import requests

def list_sheets(workbook_id, token, operator_id):
    url = f"https://api.dingtalk.com/v1.0/doc/workbooks/{workbook_id}/sheets"
    headers = {"x-acs-dingtalk-access-token": token}
    params = {"operatorId": operator_id}
    resp = requests.get(url, headers=headers, params=params)
    resp.raise_for_status()
    return resp.json()["value"]  # list of {name, id}
```

### 4.2 读取单元格范围

```python
def get_range(workbook_id, sheet_id, range_addr, token, operator_id):
    url = f"https://api.dingtalk.com/v1.0/doc/workbooks/{workbook_id}/sheets/{sheet_id}/ranges/{range_addr}"
    headers = {"x-acs-dingtalk-access-token": token}
    params = {"operatorId": operator_id}
    resp = requests.get(url, headers=headers, params=params)
    resp.raise_for_status()
    data = resp.json()
    return data["values"], data.get("backgroundColors", [])
```

**range_addr 格式**：`A1:Z100`、`B10:BG10`（单行）、`A3:C3`（部分列）

### 4.3 写入单元格范围

```python
def update_range(workbook_id, sheet_id, range_addr, values, token, operator_id, background_colors=None):
    url = f"https://api.dingtalk.com/v1.0/doc/workbooks/{workbook_id}/sheets/{sheet_id}/ranges/{range_addr}"
    headers = {"x-acs-dingtalk-access-token": token}
    params = {"operatorId": operator_id}
    body = {"values": values}
    if background_colors is not None:
        body["backgroundColors"] = background_colors
    resp = requests.put(url, headers=headers, json=body, params=params)
    resp.raise_for_status()
    return resp.json()
```

---

## 5. 关键约束与踩坑记录

### 5.1 ⚠️ 范围大小限制

| 官方文档 | 实际稳定值 | 本项目使用 |
|---------|-----------|-----------|
| 30,000 cells | ~15,600（超过开始 503） | `A:AZ × 150 = 8,850` |
| — | — | 内网环境可放宽至 `A:AZ × 200 = 10,400` |

**现象**：超过 ~15,600 个单元格后，API 返回 **503 Service Unavailable**。

**解决**：分块读取，每次最多 150 行 × 59 列（A-BG）。

### 5.2 ⚠️ PUT 值必须全部是字符串

```python
# ❌ 错误 — 数字类型
{"values": [[12345, 67]]}

# ❌ 错误 — 混合类型
{"values": [["2026/06/15", 12345]]}

# ✅ 正确 — 全部字符串
{"values": [["2026/06/15", "12345", "67"]]}
```

**错误信息**：`MissingString` 或 API 400。

**序列化规则**：
```python
def serialize(v) -> str:
    if v is None:
        return ""
    if isinstance(v, (datetime, date)):
        return v.strftime("%Y/%m/%d")
    if isinstance(v, float):
        if v == int(v):
            return str(int(v))   # 1.0 → "1"
        return str(v)
    return str(v)
```

### 5.3 ⚠️ 日期格式

钉钉表格日期存储格式为 **`YYYY/MM/DD`**（字符串），不是 Excel 序列号。

```python
# ✅ 正确
"2026/06/15"

# ❌ 错误
"2026-06-15"        # 不会被识别为日期
"2026-06-15 00:00:00"  # 不会被识别为日期
```

### 5.4 ⚠️ operatorId 必须同时出现在 GET 和 PUT

```python
# GET 和 PUT 都需要
params = {"operatorId": "${DINGTALK_OPERATOR_ID}"}
```

缺少时返回 `MissingoperatorId` 错误。

### 5.5 ⚠️ 503 重试策略

```python
for attempt in range(2):  # 最多重试 1 次
    resp = session.request(method, url, ...)
    if resp.ok:
        return resp.json()
    if resp.status_code in (503, 502):
        time.sleep(1)  # 等 1 秒
        continue
    raise RuntimeError(...)
```

### 5.6 ⚠️ 公式列保护

写入时**绝对不能覆盖公式列**。本项目通过以下机制保护：

1. **`SHEET_FORMULA_COLS`** — 预定义每个子表的公式列索引
2. **`_queue_fill`** — 标色时跳过公式列，防止写 "0" 覆盖公式
3. **`flush()`** — 按行分组，跳过公式列间隙，只写非公式列的连续区间

```python
SHEET_FORMULA_COLS = {
    "京东pop": {column_index_from_letter(c) for c, (_, f) in JD_COLUMNS.items() if f},
    "京东自营": {column_index_from_letter(c) for c, (_, f) in JD_COLUMNS.items() if f},
    "天猫旗舰店": {column_index_from_letter(c) for c in TMALL_FORMULAS},
}
```

### 5.7 ⚠️ 列范围（Douyin 专用）

抖音子表数据列可能超出 `A:AZ`，实际需要覆盖到 `BG`（第 59 列）。加载范围不足会导致未加载列的单元格被空字符串覆盖。

```python
MAX_COL = "BG"  # 覆盖抖音静态列 BF(58)/BG(59)
```

### 5.8 ⚠️ 写入失败静默问题

`flush()` 中写入失败只打印错误日志，不会阻塞后续操作。需要检查 `flush_all()` 的返回值确认所有写入成功。

```python
flush_ok = wb.flush_all()
if not flush_ok:
    print("** WARNING: Some writes to DingTalk failed **")
```

---

## 6. 代码实现解析

### 6.1 整体架构

```
fill_data_dingtalk.py
  ├── DingTalkSheetClient    — HTTP 客户端（认证 + 请求）
  ├── DTSheetProxy           — 单子表代理（读写 + 缓存 + 批量刷新）
  ├── DTWorkbookProxy        — 工作簿代理（管理多个子表）
  ├── DTCellProxy            — 单元格代理（兼容 openpyxl 接口）
  └── run_fill_dingtalk()    — 主流程编排
```

### 6.2 核心类：DingTalkSheetClient

```python
class DingTalkSheetClient:
    """封装 DingTalk Sheet API 的 HTTP 客户端"""

    def __init__(self):
        self._token = None         # 懒加载
        self._session = requests.Session()

    def _get_token(self) -> str:
        """POST /oauth2/accessToken → 获取 access_token"""
        ...

    def list_sheets(self) -> list[dict]:
        """GET /doc/workbooks/{id}/sheets → 列出所有子表"""
        ...

    def get_range(self, sheet_id, range_addr) -> dict:
        """GET .../ranges/{range} → 读取单元格"""
        ...

    def update_range(self, sheet_id, range_addr, values, background_colors=None) -> dict:
        """PUT .../ranges/{range} → 写入单元格"""
        ...
```

### 6.3 核心类：DTSheetProxy

**加载策略**：分块读取，每次 150 行，最多 6 块（900 行）

```python
class DTSheetProxy:
    CHUNK_ROWS = 150   # 每块行数
    MAX_COL = "BG"     # 最大列
    MAX_CHUNKS = 6     # 最多块数 → 900 行

    def _load(self):
        for chunk_num in range(self.MAX_CHUNKS):
            range_addr = f"A{start_row}:{self.MAX_COL}{end_row}"
            resp = self._client.get_range(self._sheet_id, range_addr)
            values = resp.get("values", [])
            if not values:
                break  # 空块 = 数据结束
            # 存入 self._cells 字典
            for r_offset, row_vals in enumerate(values):
                for c, val in enumerate(row_vals, start=1):
                    if val is not None and val != "":
                        self._cells[(row, c)] = val
```

**写入策略**：先缓存再批量刷新

```python
def _queue_write(self, row, col, value):
    """暂存写入到 self._pending"""
    self._cells[(row, col)] = value
    self._pending[(row, col)] = value

def _queue_fill(self, row, col, hex_color):
    """暂存背景色到 self._pending_fills，跳过公式列"""
    if col in self._formula_cols:
        return
    self._pending_fills[(row, col)] = hex_color
```

**刷新策略**：按行分组，按连续列区间写入

```python
def flush(self):
    """将所有 pending 写入按行+连续列区间分组，逐组 PUT"""
    for r in sorted(by_row):
        cols = sorted(by_row[r])
        # 拆分为连续区间（跳过公式列间隙）
        groups = split_contiguous(cols)
        for c_start, c_end in groups:
            self._flush_range(r, r, c_start, c_end)
```

这样避免了写入整个矩形范围时覆盖公式列的问题。

### 6.4 兼容 openpyxl 接口

```python
class DTCellProxy:
    """模拟 openpyxl 的 Cell 对象"""
    @property
    def value(self):
        return self._sheet._cells.get((self._row, self._col))

    @value.setter
    def value(self, val):
        self._sheet._queue_write(self._row, self._col, val)

    @property
    def fill(self):
        return self._fill

    @fill.setter
    def fill(self, val):
        """拦截 openpyxl PatternFill → 转为 hex 颜色"""
        self._fill = val
        if val is not None:
            rgb = val.fgColor.rgb
            if rgb.startswith("00"):
                rgb = rgb[2:]
            self._sheet._queue_fill(self._row, self._col, f"#{rgb}")
```

这样 `fill_data.py` 中的 `fill_douyin()` / `fill_jd()` / `fill_tmall()` 函数无需修改，DingTalk 版本直接复用。

### 6.5 颜色标记体系

| 颜色 | Hex | 含义 |
|------|-----|------|
| 🟡 黄 | `#FFF2CC` | 新增数据 |
| 🟠 橙 | `#FCE4D6` | 覆盖已有数据 |
| 🔴 红 | `#F4CCCC` | 删除数据 |
| 🔵 蓝 | `#D9E2F3` | 数据缺失（无源数据） |

---

## 7. 配置速查

### 7.1 钉钉应用凭证

| 配置项 | 值 |
|--------|-----|
| App Key | `${DINGTALK_APP_KEY}` |
| App Secret | `${DINGTALK_APP_SECRET}` |
| Workbook ID | `${DINGTALK_WORKBOOK_ID}` |
| Operator unionId | `${DINGTALK_OPERATOR_ID}` |
| Operator userId | `${DINGTALK_OPERATOR_USER_ID}` |
| 在线表格链接 | `https://alidocs.dingtalk.com/i/nodes/${DINGTALK_WORKBOOK_ID}` |

### 7.2 子表名称映射

| 内部名称 | 钉钉子表名 |
|---------|-----------|
| `抖音` | `抖音` |
| `京东pop` | `京东pop` |
| `京东自营` | `京东自营` |
| `天猫旗舰店` | `天猫旗舰店` |

### 7.3 运行命令

```powershell
# 干跑（只校验参数，不实际写入）
python 脚本/fill_data_dingtalk.py --start-date 2026-06-15 --end-date 2026-06-15 --dry-run

# 正式写入（所有渠道）
uv run --with openpyxl --with xlrd --with requests python 脚本/fill_data_dingtalk.py --start-date 2026-06-15 --end-date 2026-06-15

# 只写指定渠道
python 脚本/fill_data_dingtalk.py --start-date 2026-06-15 --end-date 2026-06-15 --channels douyin
python 脚本/fill_data_dingtalk.py --start-date 2026-06-15 --end-date 2026-06-15 --channels jd
python 脚本/fill_data_dingtalk.py --start-date 2026-06-15 --end-date 2026-06-15 --channels tmall

# 多个渠道
python 脚本/fill_data_dingtalk.py --start-date 2026-06-15 --end-date 2026-06-15 --channels jd,tmall
```

---

## 8. 定时任务与通知

### 8.1 定时任务（Windows Task Scheduler）

| 任务名 | 时间 | 脚本 |
|--------|------|------|
| `jd_0910` | 09:10 | `定时任务\run_jd.ps1 2026-06-24` |
| `tmall_0940` | 09:40 | `定时任务\run_tmall.ps1 2026-06-23` |
| `douyin_1330` | 13:30 | `定时任务\run_douyin.ps1 2026-06-23` |

### 8.2 日志位置

| 内容 | 路径 |
|------|------|
| 运行日志 | `C:\logs_dingtalk\YYYY-MM-DD.log` |
| 结果摘要 | `C:\logs_dingtalk\YYYY-MM-DD.result-{channel}.txt` |
| 填充总结 | `%TEMP%\fill_summary_{channel}.txt` |
| 反馈 CSV | `数据存储\采集反馈报告\写入反馈_钉钉_{start}_{end}_{channel}.csv` |

### 8.3 飞书通知

`notify_daily.py` 通过飞书 Webhook 发送交互式卡片通知，包含：
- 写入统计（新增/变更数量）
- 各子表明细
- 数据缺失项
- 日志/反馈文件路径

```powershell
python 脚本/notify_daily.py --date 2026-06-24 --channels jd --range 2026-06-22,2026-06-24
```

---

## 9. 常见问题排查

### 9.1 API 返回 503

**原因**：单次请求单元格数超过 ~15,600。

**解决**：减小 `CHUNK_ROWS` 或列范围。本项目使用 `A:AZ × 150 = 8,850`。

### 9.2 API 返回 400 / MissingString

**原因**：PUT body 的 `values` 中包含非字符串值（int、float 等）。

**解决**：所有值用 `str()` 转换后再放入 `values` 数组。

### 9.3 公式列被覆盖

**原因**：写入范围包含了公式列，非 pending 的公式列被填充了空字符串 `""`。

**解决**：
1. 使用按行+连续列区间分组写入（`flush()` 策略）
2. `SHEET_FORMULA_COLS` 中正确配置公式列
3. `_queue_fill` 跳过公式列

### 9.4 写入后数据丢失

**原因**：`MAX_COL` 不够大，未加载的列被空字符串覆盖。

**解决**：确保 `MAX_COL` 覆盖所有需要写入的列（本项目 `BG` = 第 59 列）。

### 9.5 中文乱码

**原因**：PowerShell 捕获 Python stdout 时编码不匹配。

**解决**：在 PowerShell 脚本中设置 `[Console]::OutputEncoding = [Text.Encoding]::UTF8`，或直接读取 `%TEMP%\fill_summary_*.txt` 文件。

### 9.6 dingtalk-mcp 已知 Bug

如果使用 MCP 方式（不推荐），有两个已知 bug 需要修复：

1. **URL 模板错误**：YAML 中 `?operatorId=String` 需改为 `?operatorId={operatorId}`
2. **Schema null 类型**：`listNotableRecords` 的 `value` 字段 `anyOf` 中有 `{"type": null}` → 需删除该分支

修复脚本位置：`scripts/validate-dingtalk-mcp-schema.mjs`、`scripts/stop-dingtalk-mcp.ps1`

---

## 附录：完整请求示例

### 读取子表列表

```bash
curl -X GET \
  "https://api.dingtalk.com/v1.0/doc/workbooks/${DINGTALK_WORKBOOK_ID}/sheets?operatorId=${DINGTALK_OPERATOR_ID}" \
  -H "x-acs-dingtalk-access-token: YOUR_TOKEN"
```

### 读取单元格

```bash
curl -X GET \
  "https://api.dingtalk.com/v1.0/doc/workbooks/${DINGTALK_WORKBOOK_ID}/sheets/6jsXBA/ranges/A1:Z10?operatorId=${DINGTALK_OPERATOR_ID}" \
  -H "x-acs-dingtalk-access-token: YOUR_TOKEN"
```

### 写入单元格

```bash
curl -X PUT \
  "https://api.dingtalk.com/v1.0/doc/workbooks/${DINGTALK_WORKBOOK_ID}/sheets/6jsXBA/ranges/A1:C2?operatorId=${DINGTALK_OPERATOR_ID}" \
  -H "Content-Type: application/json" \
  -H "x-acs-dingtalk-access-token: YOUR_TOKEN" \
  -d '{
    "values": [
      ["2026/07/11", "12345", "67"],
      ["2026/07/12", "23456", "89"]
    ],
    "backgroundColors": [
      ["#000000", "#FFF2CC", "#000000"],
      ["#000000", "#000000", "#000000"]
    ]
  }'
```

---

> **相关文件**：
> - 主脚本：`脚本/fill_data_dingtalk.py`
> - 配置：`脚本/config.py`
> - 通知：`脚本/notify_daily.py`
> - 项目说明：`CLAUDE.md`
> - 迁移说明：`脚本/迁移说明.md`